pipeline {
    agent any
    stages {
        stage ('BUILD') {
            steps {
                sh 'echo "My first pipeline"'
                sh "echo the box"
                sh 'ls -ltr'
            }
        }
    }
}
