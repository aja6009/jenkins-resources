pipeline {
    agent any
    stages {
        stage ('BUILD') {
            steps {
                sh 'echo "My first pipeline"'
                    echo "by this i can may usefull thinges"
                    ls -ltr
            }
        }
    }
}
